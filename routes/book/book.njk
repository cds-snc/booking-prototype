{% extends "base.njk" %}

{% block content %}

    <h1>{{ __('book.title') }}</h1>

    <div>
        <p>
            {{ __('book.intro') }}
        </p>
        <div id="calendar"></div>
        <form method="post">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            
            {{ textInput('form.firstname', 'form.firstname.desc') }}
            {{ formButtons() }}
        </form>
    </div>

{% endblock %}
