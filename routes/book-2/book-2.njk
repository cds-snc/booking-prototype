{% extends "base.njk" %}

{% block content %}

    <h1>Step 2</h1>

    <div>
        <form method="post">
            <input type="hidden" name="_csrf" value="{{ csrfToken }}">
            
            {{ textInput('form.fullname', 'form.fullname.desc') }}
            {{ textInput('form.email', 'form.email.desc') }}
            {{ formButtons() }}
        </form>
    </div>

{% endblock %}
